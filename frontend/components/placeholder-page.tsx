import { Construction } from "lucide-react";
import { AppShell } from "./app-shell";
import { Card, PageHeading, StatusBadge } from "./ui";
export function PlaceholderPage({ title, description }: { title: string; description: string }) { return <AppShell title={title}><PageHeading title={title} description={description} /><Card className="mt-8 max-w-2xl"><StatusBadge>Foundation phase</StatusBadge><div className="mt-5 flex gap-4"><span className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-blue-500/10 border border-blue-500/20 text-blue-400"><Construction aria-hidden="true" size={21} /></span><div><h2 className="font-semibold text-white">This module is not implemented yet</h2><p className="mt-1.5 text-sm leading-6 text-slate-500">Its workspace will be added in a later phase. No live farm data, recommendations, or controls are connected here.</p></div></div></Card></AppShell>; }
